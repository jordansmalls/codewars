// Square Function
/*
Now you have to write a function that takes an argument and returns the square of it.
*/

const square = n => n**2;

const squareAgain = n => Math.pow(n, 2);

function squareAgainAgain(n){
    return n * n
}

const squareAgainAgainAgain = num => {
    if(typeof num === 'number')return num**2;
    else return undefined
};


// all tests pass =)